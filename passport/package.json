{
  "name": "passport",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "ts-node src/server.ts",
    "start": "node server.js",
    "build": "tsc -p .",
    "clean": "rimraf \"dist\"",
    "prepare": "copyfiles app.yaml dist; copyfiles package.json dist",
    "deploy": "gcloud app deploy ./dist --quiet",
    "build_deploy": "npm run clean; npm run build; npm run prepare; npm run deploy"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "pug": "^2.0.4",
    "ts-node": "^8.4.1",
    "typescript": "^3.6.3",
    "uuid": "^8.1.0"
  },
  "devDependencies": {
    "copyfiles": "^2.3.0",
    "rimraf": "^3.0.2",
    "@types/express": "^4.17.1",
    "@types/node": "^12.7.8",
    "@types/uuid": "^8.0.0"
  }
}
