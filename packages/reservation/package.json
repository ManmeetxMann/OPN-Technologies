{
    "name": "@opn/reservation",
    "private": true,
    "version": "1.5.1",
    "scripts": {
        "start": "node reservation/src/server.js",
        "test": "jest",
        "test:coverage": "jest --coverage",
        "dev": "ts-node src/server.ts",
        "dev:debug": "nodemon --inspect=3008 src/server.ts",
        "dev:local": "FIRESTORE_EMULATOR_HOST=localhost:8080 PUBSUB_EMULATOR_HOST=localhost:8085 FEATURE_DEBUG_MANUAL_TIMESTAMPS=enabled DEBUG_GUILIBLE_MODE=enabled ts-node src/server.ts",
        "clean": "rimraf -rf ./dist",
        "prepare": "copyfiles ../common/.env dist/common && copyfiles app.yaml dist && copyfiles openapi.yaml dist && copyfiles package.json dist && node ../../tools/package-cleaner.js reservation start",
        "add_static_assets": "copyfiles ./src/views/* dist/reservation/ && copyfiles ./src/views/layouts/* dist/reservation/ && copyfiles ./src/static/js/* dist/reservation/ && copyfiles ./src/static/images/* dist/reservation/ && copyfiles ./src/templates/* dist/reservation/ && copyfiles ../common/src/static/fonts/* dist/common/",
        "compile": "tsc -p tsconfig.build.json",
        "build": "npm run clean && npm run compile && npm run prepare && npm run add_static_assets",
        "gcloud_deploy": "gcloud app deploy ./dist --quiet",
        "deploy": "npm run build && npm run gcloud_deploy",
        "initialize": "ts-node -e \"require('./src/server.ts').init()\""
    },
    "dependencies": {
        "@google-cloud/debug-agent":"5.1.3"
    },
    "devDependencies": {
        "nodemon": "^2.0.4",
        "jest": "^26.6.3",
        "ts-jest": "^26.1.1"
    }
}
